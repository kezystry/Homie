# Deploy

Per-node configuration and the encrypted mesh.

- Node roles (perception / reasoning) and their addresses.
- Mesh keys and peer links (encrypted; never committed).
- Secrets handling — per-tile, local-only by default.

Secrets and keys live outside version control. See [`../.gitignore`](../.gitignore).
